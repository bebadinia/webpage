services:
  portfolio:
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    image: webpage-portfolio:local
    restart: unless-stopped
    ports:
      - "127.0.0.1:8085:80"
